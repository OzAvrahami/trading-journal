# Trading Journal v1.1.0 - Fresh Neon Initialization

Trading Journal now runs on a freshly initialized Neon PostgreSQL database, with
verified connection security, application-owned authentication, live Finnhub pricing
and manual fallback. This release also corrects account-scoped CSV import detection,
stale quote handling, Hebrew pagination and proxy-aware rate limiting.

The implementation was deployed and production-verified on 2026-09-14 at
[c6ad89638809e5ab350c3d7614b3baf902e2b3c7](https://github.com/OzAvrahami/trading-journal/commit/c6ad89638809e5ab350c3d7614b3baf902e2b3c7).
All first-party packages and lockfiles are 1.1.0. The final release commit also
contains the reviewed release documentation; the implementation SHA above is
verification provenance, not a substitute for verifying the final release tag.

## Fresh Neon production

- Start independently on Neon PostgreSQL 18 from reviewed repository migrations
  001-020, with pgcrypto, explicit bootstrap ownership/search path, separate
  migrator/backend logins and tj_owner access, verified TLS and bounded connections.
- This is an accepted **fresh start**, not a transfer of historical Supabase data.
  Existing users register anew; old sessions/accounts/trades/investments were not
  imported. All new Neon records are protected. Validation fixtures were not copied
  into production, and Supabase is not a recovery target.
- Retain pg and application-owned JWT authentication; remove the confirmed unused
  Supabase wrapper/SDK and exclusive dependencies.

## Connection, initialization and authentication reliability

- Use explicit certificate/hostname verification, reject conflicting or weakening
  SSL URL options, preserve intentional loopback development and date-only parsing,
  and use a separate direct migration connection with explicit role selection.
- Serialize migrations, respect existing SQL transaction boundaries, atomically
  record successful files/checksums and preserve historical migration records.
  Add forward constraint validation and global/schema default-privilege hardening.
- Preserve policy-free RLS and server ownership checks. The trusted runtime assumes
  owner; this is not a least-privilege per-user database RLS model.
- Reject nonexistent-user sessions before protected work, clear invalid refresh
  cookies and stale client token/user/query state, share refreshes and ignore late
  responses across user changes. Network/server failures do not force invalidation.

## Fixes

- Correct proxy-shared rate-limit buckets. Use Railway's edge-overwritten X-Real-IP
  only when production Railway deployment metadata is present, and socket identity
  otherwise. Ignore forwarded chains, fail malformed headers into the peer bucket,
  normalize mapped IPv4 and IPv6 spellings, and aggregate IPv6 /64 addresses.
  Keep Express proxy trust disabled and preserve global 300/auth 20 requests per
  15 minutes, credentialed CORS and production cookie policy. This assumes HTTP-edge
  public ingress and a trusted private project network; no public TCP proxy is
  configured. Do not admit untrusted callers onto that private network.

- Allow the same CSV file in different owned accounts while retaining within-account
  duplicate protection; align preview feedback and forward migration 020.
- Reject quotes older than 96 hours or more than 5 minutes in the future. On failed
  refresh, use dated manual values instead of retaining an earlier quote as live;
  preserve explicit unavailable states. Live Finnhub integration remains included.
- Translate trade pagination into Hebrew and respect the selected text direction.
- The administrator company/broker catalogue, including casing requirements, is
  [Feature #7](https://github.com/OzAvrahami/trading-journal/issues/7), independently
  planned and excluded from this release.

## Verification and limits

TJ-02 local connection/migration/authentication tests, TJ-03 independent Neon
initializations/security checks, TJ-04 deterministic authenticated API/financial,
real-browser and isolated recovery evidence are recorded in the existing reports.
TJ-04 implementation paths/hashes were verified against the pinned pre-commit
manifest. No accepted broad suite was repeated for release documentation.

TJ-05 production checks verified actual running role/TLS/database, registration,
first account, login/reload/refresh/logout, Secure/HttpOnly/SameSite=None cookies,
credentialed CORS, two-user isolation, live Finnhub and manual-price fallback.
The owner explicitly accepted production on **2026-09-14**. This records production
acceptance, not a continuous-monitoring guarantee.

Bounded Railway metrics from September 13 19:31:55Z to September 14 11:36:08Z report
327 API requests, no 5xx, p50/p95 180/350ms; this is not a 24-hour soak or SLA.
Stabilization found and locally corrected a **proxy-aware rate-limiting defect**.
Eight focused HTTP/address/security tests and seven existing focused authentication
tests pass. The owner supplied verified two-network counter evidence for the deployed correction:
cloud 297/296/295 despite forged identity headers, PC 299/298, then cloud 294 within
its original window. These completed probes were not repeated. Post-deployment logs
through 12:52:50Z contain no previous proxy/rate-limit warnings in the retrieved sample.

Two earlier encrypted archives restored to restricted Neon targets with data,
ledger and effective schema/security matches; the accepted app read the latest
restored copy with user isolation. A newer September 14 archive includes owner
onboarding/imports, passes byte/catalog readback and has not separately been restored.
A rollback-only temporary-table drill demonstrated final-snapshot preservation of
later inserts/updates/deletes, refresh revocation and import dedup markers. It is
not an end-to-end PITR or authenticated recovery drill. Off-device durability,
unattended scheduling, full-window PITR, arbitrary divergence reconciliation and
RPO/RTO guarantees remain unproven. Neon remains Free; no billing change is included.

## Verified production results

Both Railway services ran the verified implementation successfully. The actual API
pool used production `trading_journal`, `tj_backend` assuming `tj_owner`, the intended
direct endpoint and authorized TLSv1.3. Focused HTTPS checks passed login, authenticated
account reads, refresh rotation, logout and cross-user access rejection, with
Secure/HttpOnly/SameSite=None cookies and exact-origin credentialed CORS.

Live Finnhub returned AAPL at 332.27 (provider timestamp 2026-09-11T20:00:00Z, within
the freshness policy when checked). An unavailable synthetic symbol retained the
existing manual USD valuation: cash 0, holdings 100, total 100 and unrealized PnL 0,
matching the expected values. All 20 table counts and row fingerprints matched
before and after; existing production records were preserved.

The post-deployment log sample through 2026-09-14T12:52:50Z contained 33 API and
7 client records with no previous proxy/rate-limit warnings or error-like lines.
These are bounded observations, not continuous monitoring or a future guarantee.

## Separate operational follow-up

The owner authorized proceeding with v1.1.0 and deferred unfinished backup/recovery
requirements to [Establish independent production backups and verify recovery #8](https://github.com/OzAvrahami/trading-journal/issues/8).
Off-device storage/key custody, scheduling/retention/alerts, full-window PITR and
comprehensive post-recovery-point reconciliation remain separate work, not successful
tests or release blockers. The previously demonstrated archive restores and limited
drill do not establish zero-loss or recovery-time guarantees. All new Neon records
must remain protected.
