# TJ-04 isolated application and recovery verification — 2026-09-12

**Current update, 2026-09-13:** the owner accepted the application review/tests.
Only the final reviewed/pushed commit verification remains pending for #4. The
execution narrative below is retained as dated evidence, including superseded
browser/acceptance gaps. See the [current handoff](tj-04-handoff-2026-09-13.md).

TJ-04 is reviewable and remains **open / Verify**. Authenticated Neon API checks,
deterministic financial results, focused client regressions and the isolated
recovery exercise passed within the limits below. Interactive browser acceptance
is still open. Two demonstrated product failures required uncommitted fixes.
No production readiness or final implementation commit is claimed.

The [machine-readable evidence](tj-04-neon-2026-09-12.json) retains initial failures,
corrections, passing follow-ups, API responses containing only synthetic data,
fixture identifiers, catalogs, full-precision ledgers and resource readbacks.
[Independent fixture expectations](tj-04-fixtures-2026-09-12.md) were specified
before writes; subsequent harness/precision clarifications are explicitly dated
as execution clarifications. Assertion and scenario counts overlap; they are not
a count of independent end-to-end tests.

## Starting state and TJ-03 acceptance

Local branch `chore/tj-02-neon-preparation`, HEAD and remote branch matched
`2821435dc609ad85d3447cd22f799fb17a508968`; main matched
`9434cda9b8b225b6615e31bad379c3de63d4ea4e`. The index was empty. Four modified
governance/operations documents and the two untracked TJ-03 evidence files were
reviewed in full and preserved. No applicable AGENTS.md was found in the checked
repository/ancestor paths. The required standard, plan, guide, backlog and issues
were read before execution.

Under the owner's delegated instruction, the agent accepted TJ-03's isolated
scope after checking the detailed artifact evidence: both 19-file ledgers and
checksums, normalized schema equality, empty business tables, security/probes,
interruption limits and the requirement to issue database ACL statements as
`tj_owner`. The completed suite was not rerun. This is agent artifact review,
**not a claim that the owner or this chat independently inspected local artifacts**.
Issue #3 was closed as completed and its existing Project item set/read back Done.
Issue #4 moved Ready → In Progress → Verify. Original TJ-03 evidence files remain
byte-for-byte unchanged as historical execution snapshots; their former issue
states are superseded here. Issues #5/#6 remain Backlog.

## Destination and fixture boundary

| Setting | Actual value |
| --- | --- |
| Organization / plan | Oz / `org-lively-cherry-67205699`, API plan `free` |
| Project / region / version | `trading-journal` / `delicate-fire-79141899`, AWS Oregon `aws-us-west-2`, PostgreSQL 18.6, pgcrypto 1.4 |
| Validation branch / endpoint | `br-green-cake-arg1asaf` / `ep-bold-fire-aryb3eiv.c-4.us-west-2.aws.neon.tech` |
| Application test database | `trading_journal_validation`, owner `tj_owner`; 20 migrations after the TJ-04 forward correction |
| Preserved independent database | `trading_journal_validation_repeat`, original 19 migrations and zero business rows; final inspection used an explicit read-only transaction |
| Runtime identity | Login `tj_backend`, startup effective role `tj_owner`, direct endpoint, verified certificate and hostname TLS |
| Migration identity | Separate `tj_migrator` direct connection, effective `tj_owner`, reviewed serialized runner |
| Fixture run | `tj04-20260912-5d8581`; two unique `@example.test` users, IDs in JSON evidence |

The local Express application bound only to an ephemeral `127.0.0.1` port.
Protected DPAPI credentials and a task-only signing key were loaded in memory;
inherited database/provider settings were cleared before selecting the explicit
validation destination. Production secrets/configuration were not changed.
Every fixture creation was confined to the application validation database.
The starting inventory had zero rows in all 19 business tables. Final ownership
checks found only the two task users and their records. No non-test data existed
or was removed. API deletion tests removed only named task fixtures; the temporary
import-failure trigger/function were removed in a finally block.

Final retained rows: 2 users, 24 refresh sessions, 7 accounts, 10 trades, 10
journals, 1 journal/trade link, 1 daily-review detail, 1 rule, 3 checks, 1 goal,
1 strategy, 1 setup, 1 preference, 6 import runs, 8 import rows, 3 portfolios,
2 instruments, 8 investment transactions and 1 manual price. IDs and per-table
fingerprints are recorded, including automatically created sessions/history.
These are validation fixtures, never a production initialization source.

## Acceptance mapping and actual results

| Issue #4 item | Evidence / disposition |
| --- | --- |
| 1. Independent inputs, expected outputs, counts and tolerances | Complete: fixture document, initial/final inventories, API expected/actual comparisons. Investment money uses cents; PnL uses four decimals; prices/quantities use eight where declared. Exact DATE/instant assertions. |
| 2. Registration/authentication, cleanup, negative isolation | Real API registration, empty accounts/portfolios, login, refresh rotation/replay, logout, stale-cookie and nonexistent-user token rejection passed. Concurrent refresh produced one 200 and one 401; the winning token worked. Injected database dependency failure returned 500 without cookie clearing and the same access token worked after recovery. Two-user negative reads/writes/relationship IDs/imports/exports passed. Client cancellation/cache/refresh-loop and transient-error behavior passed focused jsdom tests. **Combined acceptance remains open for real-browser cookie/cache/session verification.** |
| 3. First account/portfolio, trading and imports | Complete available API checks: explicit onboarding, open/closed/short trade CRUD, fee edits, net/R/duration, filters, quoted CSV export; TopstepX and Tradovate (the complete importer registry), partial-fill grouping/physical lineage, parse-session isolation, account-scoped duplicate file/trade protection after correction. Conflicting execution pair rejected. A task-scoped database trigger forced the second trade insertion to fail: HTTP 500, zero trades committed, failed history retained for both rows; trigger removed. |
| 4. Journal/review/rule/goal/strategy context | Complete API checks: linked journal Unicode create/read/update/delete; foreign relationships denied; structured daily review isolation; followed/broken/not-applicable checks produced 50% adherence with denominator 2; goal progress matched two closed trades before later fixtures; strategy/setup ownership and updates passed. |
| 5. Investments and price behavior | Complete deterministic checks: ledger chronology, failed oversell/overspend/deposit removal without changed balances, foreign instrument/portfolio/transaction/price denial, manual/missing/provider/failure paths, currency-separated overview/performance/dividend/allocation. Actual Finnhub connectivity is **not tested**; provider responses were simulated without outbound requests. |
| 6. Languages, RTL, dates/timezones/precision | API English/Hebrew preference and Hebrew text persistence passed. DATE values survived a changed SQL timezone and user timezone. Offset TIMESTAMPTZ instants, Jerusalem spring DST and New York repeated fall hour each retained 60-minute duration. User-local midnight/year, Sunday/Monday/month boundaries matched filtered calendar dates. Eight-decimal price/four-decimal PnL storage and invalid calendar/range rejection passed. **Interactive English/Hebrew/RTL, responsive forms/navigation and empty-state rendering remain open.** |
| 7. Results, IDs, failures and recovery; resolve launch blockers | Results and recovery evidence complete below. **Combined item remains open:** browser acceptance and review/publication of the fixes must precede TJ-05. No fixture promotion or launch authorized. |

The connected browser inventory was empty (`No browser is available`, then `[]`).
No browser workflow was invented or replaced by a claim about jsdom rendering.
There were no workflow test issues and no investigation of Project automation.

### Financial expectations versus authenticated API results

All values below matched exactly at their stated precision; no live market price
was used to generate an expected result.

| Case | Independently expected | Actual |
| --- | --- | --- |
| Long 100→110 ×2, fee 1, risk 10 | Gross 20; net 19; R 1.9 | 20; 19; 1.9 |
| Short 110→100 ×2, fee 1, risk 10 | Gross 20; net 19; R 1.9 | 20; 19; 1.9 |
| Long fee changed to 2 then restored | Net/R 18/1.8 then 19/1.9 | Matched |
| USD deposit 1000, buy 2×100, price 110 | Cash 800; holdings 220; total 1020 | 800; 220; 1020 |
| Further buy 2×120, fee 4 | Cash 556; basis 444; average 111 | Matched |
| Sell 1×130, fee 2 | Cash 684; remaining basis 333; realized 17 | Matched |
| Dividend 9, fee 1; standalone fee 3; withdraw 100 | Cash 589; holdings 330; basis 333; realized 17; unrealized −3; fees 10; gross dividends 9; total 919; net contributions 900; return 19 | All matched |
| USD dated performance, Jan 1–7 | 1000, 1020, 996, 1014, 1022, 1019, 919 | Matched |
| Separate EUR deposit | EUR 500 throughout, never summed with USD | Matched |
| TopstepX supplied gross 12, fees 1 + commission 2 | Net 9 | 9 |
| Tradovate two partial fills, total quantity 3 | Gross 40; fees 5.70; net 34.30; one trade linked to two physical rows | Matched |
| Four boundary trades, each gross 3 less fee .01 | Account total 11.96; average R 2.99 | 11.96; 2.99 |

Account-filtered calendar dates were `2026-01-01`, `2026-02-02`, `2026-03-27`
and `2026-11-01`, each USD 2.99 in Jerusalem. Unscoped daily summaries explicitly
report mixed EUR/USD and suppress money totals; counts/win rate remain available.
Manual price dates remain visible, including deliberately old fixture prices.

## Demonstrated failures and corrections

1. **Account-wide file duplication:** the reviewed SHA returned 409 when A imported
   the same file into a second owned account. The service lookup and preview now
   scope blocking to the selected account, and forward migration 020 replaces
   the successful-file unique index with `(user_id, account_id, file_sha256)`.
   Same-account duplicates still return 409; the other account inserts one trade.
   A transactional duplicate insert also confirmed SQLSTATE 23505. All original
   001–019 ledger timestamps/checksums remain unchanged; RLS/security audit passed.
2. **Stale market quote:** a simulated January observation was accepted as current
   in September. The provider service now rejects observations older than 96 hours
   or more than five minutes in the future. The ceiling accommodates a long market
   closure; it is not an exchange-calendar guarantee. A failed refresh now clears
   the usable quote lookup instead of showing React Query's retained data as live.
   The authenticated stale response is 503; manual fallback remains 330/−3 for the
   fixture. Regression tests cover exact age boundaries and retained-data fallback.

No historical migration was edited. The initial core/investment/relationship/
import results use committed SHA `2821435...`. The account fix, migration 020,
later quote fix and their reruns use the **uncommitted working tree** on that SHA.
The first market phase had the import correction but the original quote behavior;
the later market phase passed with the quote correction. File SHA-256 hashes are
recorded in the JSON; no final implementation commit exists.

Harness corrections are retained separately from product failures: portfolios
return an envelope, trade counts use `pagination.total`, analytics uses `totals`,
price text has eight decimal places, and mixed-currency day money is null. Tests
were corrected without changing application contracts or deleting their fixtures.

Final review also applied the freshness limit to already-cached provider quotes.
The boundary test advances the clock after caching an exactly-96-hour quote and
verifies rejection; all 13 market service tests passed again. Authenticated API
evidence precedes this final cache-boundary guard; no cloud suite was repeated.

Focused checked-in regressions: **21 server tests and 14 client tests passed**
(import history, market service, Import page, quote hook, API session and
AuthContext). Only affected/focused checks ran. The client run emitted existing
Vite plugin deprecation warnings. No broad application suite was repeated.

## Isolated recovery exercise

Free plan, included branch allowance (10), project storage below 512 MiB, six-hour
history configuration and minimal fixed 0.25-CU compute were read before creation.
There was one branch; the new recovery child made two. Free defaults automatically
suspend after five idle minutes (`suspend_timeout_seconds=0` means plan default).
No subscription, paid add-on, snapshot, resource deletion or production target
was used. See [Neon plans](https://neon.com/docs/introduction/plans) and
[branch creation API](https://api-docs.neon.tech/reference/createprojectbranch).

| Recovery observation | Actual |
| --- | --- |
| Source database timestamp / WAL observation | `2026-09-12 13:55:19.539356+00` / `0/2987CC8` |
| Requested timestamp | `2026-09-12T13:55:19.539Z` |
| Recovery branch | `recovery-validation-tj04-20260912-5d8581` / `br-soft-mountain-aratb02z` |
| Endpoint | `ep-aged-rain-ar8t6z4b.c-4.us-west-2.aws.neon.tech`, fixed 0.25 CU, default automatic suspension |
| Provider readback of parent point | `2026-09-12T13:55:18Z`, LSN `0/2987FE8` |
| Included pre-point journal | `ac2000d7-6b5d-4a10-9e4e-a9e22694d438` |
| Excluded later journal | `7e3828ec-b4a2-449f-b0ee-795413c822b3`; retained on source root |
| Fork request / selected history age | `13:55:35.012Z`; requested point about 15.473 seconds earlier |
| Operations finished / first verified TLS SQL | 7.231 s / 8.427 s after request, measured from this workstation |
| Data/catalog inspection / final application evidence | Initial inspection 19.917 s; provider-default difference resolved and application evidence complete at `13:57:37.232Z` |

All 19 business-table fingerprints matched the selected source snapshot. The
pre-point marker was readable through the actual application on the child; the
later marker returned 404. Restored portfolio values were cash 589, market 330,
total 919, realized 17 and unrealized −3. The full-precision 20-record ledger
matched. Both source markers and every acknowledged source row were retained.

An initially strict catalog comparison exposed two provider-created `cloud_admin`
default grants to `neon_superuser`, for future provider-owned public tables and
sequences. The exact two differences are in the follow-up evidence. Every other
normalized schema/security component matched; application defaults remained
hardened. Role membership, backend startup role, denied untrusted connection and
denied login-only table access were checked. No ACL or RLS was weakened to pass.

The provider's reported timestamp/LSN differs from the literal requested point;
the pre/post data oracle passed, but subsecond alignment is not claimed. The
**tested interval is the recorded short interval**, not the full configured six
hours. The child implicitly includes copies of the root's admin and repeat
databases; neither was written by this task. At final readback the child endpoint
was idle; the root was active. Meter counters are delayed usage observations,
not proof of zero consumption or a financial commitment.

This exercise does not merge post-point writes, preserve later session revocations
automatically, demonstrate a full outage recovery, or establish zero loss/RPO/RTO.
Future recovery must retain and reconcile all new acknowledged records and session
state before reopening writes. Supabase is not a recovery target.

## Review handoff and remaining gates

Review the two uncommitted fixes, migration 020 and evidence. Complete real-browser
English/Hebrew/RTL and responsive onboarding/trade/import/investment flows plus
cookie/cache/logout/race behavior when a browser is available. No repeat TJ-03
setup or obsolete source access is needed. Owner acceptance and the final pushed
SHA remain pending; issue #4 stays open. TJ-05/#5 and TJ-06/#6 stay Backlog.

Production requires a separately initialized empty destination at the final
reviewed code revision, never this validation database or recovery child. Railway
latency, cold-start/load sizing, live provider configuration, retention/recovery
commitments, secret rotation and launch remain separate execution gates. No
staging, commits, pushes, PRs, tags, releases, versions, Railway configuration,
production initialization or deployment changed during this task.

## Pending file inventory and final configuration checks

The index is empty. Local HEAD and both remote branch tips were rechecked and
remain the expected SHAs. UTF-8/JSON parsing, local documentation links,
`git diff --check`, actual credential-value/JWT scanning and package/lockfile
version checks passed. Three pre-existing whitespace lines were retained; no new
whitespace error was introduced. No task helper server remains running.

This pass changed the following 17 files (the four existing operations/governance
files retain their pre-existing TJ-03 work):

- `CHANGELOG.md`
- `client/src/hooks/useInvestmentQuotes.js`
- `client/src/hooks/useInvestmentQuotes.test.jsx`
- `client/src/pages/Import.jsx`
- `client/src/pages/Import.test.jsx`
- `docs/github-development-standard.md`
- `docs/neon-initialization.md`
- `docs/neon-migration-backlog.md`
- `docs/neon-migration-plan.md`
- `docs/validation/tj-04-fixtures-2026-09-12.md`
- `docs/validation/tj-04-neon-2026-09-12.json`
- `docs/validation/tj-04-neon-2026-09-12.md`
- `server/src/db/migrations/020_account_scoped_import_files.sql`
- `server/src/services/importHistoryService.js`
- `server/src/services/importHistoryService.test.js`
- `server/src/services/marketDataService.js`
- `server/src/services/marketDataService.test.js`

Two additional previously untracked files remain pending unchanged:
`docs/validation/tj-03-neon-2026-09-12.md` and its `.json` evidence file.
Thus the complete pending inventory is 19 paths. No actual `.env`, credential,
raw CSV upload, test log or generated runtime artifact is included. Protected
credentials remain outside Git; task helpers/logs are under ignored `.tmp/tj04`.

## Subsequent browser evidence - 2026-09-13

The sections above retain the September 12 execution snapshot, including its
browser gap, 19-path inventory and stopped helper process. The subsequent
[real-browser report](tj-04-browser-2026-09-13.md) and
[structured evidence](tj-04-browser-2026-09-13.json) supersede those current-state
limits: 21 Chrome scenarios and 15 focused trade/localization tests passed. The
actual client/API remain running for owner visual review. Three additional source
files correct the demonstrated Hebrew pagination fallback; earlier fixes and
migration 020 are unchanged. Issue items 2/6 are supported; item 7 remains open
pending owner visual acceptance and final publication. Production HTTPS cookies
and actual Finnhub connectivity remain downstream. No earlier API/recovery test
result was rewritten, and no owner acceptance is inferred.
