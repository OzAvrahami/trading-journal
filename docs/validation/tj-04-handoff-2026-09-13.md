# TJ-04 accepted application review and publication handoff — 2026-09-13

**Owner application review and TJ-04 test acceptance are received.** The final
reviewed/pushed implementation commit has not yet been created or verified.
Keep #4 open/Verify; after exact remote-commit verification and satisfaction of
the remaining #4 criteria, close it/Done and move #5/Ready. #1–#3 remain closed/Done,
#5/#6 Backlog. This is not production readiness or authorization to deploy.

## Current state and review

Repository `OzAvrahami/trading-journal`, branch `chore/tj-02-neon-preparation`,
local HEAD and remote branch `2821435dc609ad85d3447cd22f799fb17a508968`.
Remote main remains published v1.0.0 at
`9434cda9b8b225b6615e31bad379c3de63d4ea4e`. The index is empty. The actual
tracked/untracked contents and current diff were reviewed afresh; the inventory
below is not an assumption based on the earlier 19/27-path handoffs.

The review covers TJ-03 initialization/ACL correction and schema/security evidence;
forward migration 020 and account-scoped CSV duplicate handling; stale provider
observation rejection and client manual fallback; Hebrew pagination; their focused
tests; fixture specifications, API/browser/recovery evidence and three screenshots;
CHANGELOG and governance/operations reconciliation. No catalogue/admin/casing
implementation or unrelated code was found. No application code was changed during
this acceptance/publication preparation.

Prior release-publication corrections remain intact. Baseline CHANGELOG content,
`docs/release-baseline.md` and `docs/releases/v1.0.0.md` are unchanged in this pass;
the latter two are already committed at the current base and are not added to the
pending inventory. Historical migration files 001–019 match HEAD; all nine root/
client/server manifest and lockfile version locations remain 1.0.0. Migration 020
adds no tables or data changes: it replaces the successful-import index with
`(user_id, account_id, file_sha256)` while retaining the success-status predicate.
The runner applies the index replacement and ledger atomically. Its normalized
checksum matches the recorded validation ledger; no migration was executed now.

## Evidence and limits

- [TJ-03 report](tj-03-neon-2026-09-12.md) and [JSON](tj-03-neon-2026-09-12.json):
  independent empty initialization, role/TLS/ownership checks, corrected owner-issued
  database ACLs, normalized schema/security equality, ledger reruns and interruption.
- [TJ-04 API/recovery report](tj-04-neon-2026-09-12.md),
  [JSON](tj-04-neon-2026-09-12.json) and [expectations](tj-04-fixtures-2026-09-12.md):
  initial failures remain distinguished from corrections and passing reruns.
  Long oracle gross/net/R 20/19/1.9; investment initial cash/holdings/total
  800/220/1020; extended cash/holdings/total 589/330/919, realized 17/unrealized −3.
- [Browser report](tj-04-browser-2026-09-13.md) and
  [JSON](tj-04-browser-2026-09-13.json): 21 Chrome scenarios passed, zero page errors;
  real session/cache races, temporary failures, RTL/mobile, duplicate preview and
  controlled quote fallback. The 15 focused trade/localization tests passed.
  Earlier focused evidence records 21 server tests and 14 client tests; counts
  refer to their documented scopes, not a new combined suite run.

Current source fingerprints match the successful recorded implementation evidence.
No new code change or concrete uncovered regression justified rerunning the completed
API/browser/financial/recovery suites. The review checked UTF-8, JSON, Markdown links,
evidence/hash references, whitespace, Git blob identities and private-value exclusion.
Screenshots were visually inspected; emails remain masked. No real `.env`, credentials,
private review file, backup, runtime helper/log, build output or provider data dump
belongs to the allowlist. Historical raw evidence remains dated; owner acceptance
does not rewrite an earlier failed attempt into a passing test.

The six-hour Neon history setting is not a demonstrated six-hour recovery window,
zero-loss promise or RPO/RTO commitment. Production HTTPS cookies, actual Finnhub,
Railway-to-Neon latency/load, production initialization and operational commitments
remain TJ-05/TJ-06 gates. All existing Neon records, including the owner's newly
registered user and accounts, are protected. No database queries/writes, fixture
cleanup, resource provisioning, production secret rotation or deployment occurred
in this handoff. The Supabase fresh-start decision remains accepted.

## GitHub corrections completed

Paginated enumeration found 19 items in Trading Journal Project #12 (two pages)
and 45 in Finance Tracker Project #1 (five pages). The latter's actual repository
association is `OzAvrahami/finance-tracker`; its contents and each affected issue's
memberships confirmed the destination. Before removal, the local snapshot captured
all field definitions/values, explicitly representing unset fields as null, and
issue bodies, identities, labels, milestones, dependencies and memberships.

Finance Tracker #36–#47 already had exactly one correct membership in
[Finance Tracker #1](https://github.com/users/OzAvrahami/projects/1). Only their
12 erroneous memberships in [Trading Journal #12](https://github.com/users/OzAvrahami/projects/12)
were removed. No items were recreated or added to the correct project, no fields
were copied, and no underlying issue was transferred, deleted, closed or modified.
Full readback matched all correct Project fields, including unset values, and
underlying metadata. Trading Journal #1–#7 remained present. The owner-confirmed
workflow correction was not reopened or tested.

At the immediate cleanup readback, Finance Tracker Status/Priority values remained: #36 Backlog/P1;
#37/#38 Done/P1; #39 Verify/P1; #40 Backlog/P2; #41 Backlog/P1; #42–#47 Backlog/P2.
Complete before/after snapshots remain in ignored `.tmp/tj04-handoff/` for local
review; the [handoff JSON](tj-04-handoff-2026-09-13.json) contains a concise outcome.

Feature [#7](https://github.com/OzAvrahami/trading-journal/issues/7) is now titled
**[Feature]: Manage a central company and broker catalogue through administrator settings**.
Its detailed body is unchanged; labels are feature/frontend/backend/database,
one Project #12 membership, Backlog/P2, no milestone/version/date/dependencies.
It remains independent of the Neon sequence. Its casing requirement was not split
into a Bug and no feature implementation or release scheduling was started.

Issue #4 records the explicit owner acceptance, with the final pushed-commit gate
still open. Existing milestone, labels, priorities and dependency order are retained.
The historical visual-acceptance-pending notes are clearly superseded.

## Production preparation and owner publication

The [TJ-05 launch plan](../neon-launch-plan.md) records fresh authenticated Railway
configuration evidence: one production environment; both source triggers on main;
PR environments disabled; no preparation-branch trigger. Both latest deployments
still report the v1.0.0 SHA. API settings still point to inactive Supabase and lack
the Neon role/explicit TLS settings and Finnhub key. These values were inspected
without exposing credentials. Current deployment metadata does not establish a
working authenticated production app.

Only the dedicated preparation branch may be pushed in this handoff. Do not update
main: it is a deployment trigger. The owner publication scripts validate the repo,
branch, base, remote, empty initial index, explicit allowlist and freshly reviewed
Git blob identities; display the staged diff; create a separate normal commit;
push only the preparation ref without tags/force; and compare the full remote SHA.
Each script is self-contained and stops on failures. They and the immutable review
manifest are private local handoff files, not application source or credentials.
They were prepared and syntax-checked, not executed for staging/commit/push.

After the owner pushes, verify that exact remote commit's parent, changed paths,
blob IDs and evidence match the review manifest, including unchanged files outside
the allowlist. Equality of HEAD and remote SHA alone is insufficient. Only then
finish the remaining #4 criterion and advance #5 to Ready. Production execution
still requires the separately documented target/configuration/recovery gates.

## Explicit reviewed file inventory

The following 30 paths comprise the pending owner commit. Current hashes are in
the private local review manifest; the handoff JSON identifies reviewed source
hashes and validation results. No final implementation commit SHA is claimed.

```text
CHANGELOG.md
client/src/hooks/useInvestmentQuotes.js
client/src/hooks/useInvestmentQuotes.test.jsx
client/src/i18n/locales/en.js
client/src/i18n/locales/he.js
client/src/pages/Import.jsx
client/src/pages/Import.test.jsx
client/src/pages/Trades.jsx
docs/github-development-standard.md
docs/neon-initialization.md
docs/neon-launch-plan.md
docs/neon-migration-backlog.md
docs/neon-migration-plan.md
docs/validation/tj-03-neon-2026-09-12.json
docs/validation/tj-03-neon-2026-09-12.md
docs/validation/tj-04-browser-2026-09-13.json
docs/validation/tj-04-browser-2026-09-13.md
docs/validation/tj-04-browser-2026-09-13/accounts-he-desktop.png
docs/validation/tj-04-browser-2026-09-13/holdings-he-fallback.png
docs/validation/tj-04-browser-2026-09-13/trades-he-mobile.png
docs/validation/tj-04-fixtures-2026-09-12.md
docs/validation/tj-04-handoff-2026-09-13.json
docs/validation/tj-04-handoff-2026-09-13.md
docs/validation/tj-04-neon-2026-09-12.json
docs/validation/tj-04-neon-2026-09-12.md
server/src/db/migrations/020_account_scoped_import_files.sql
server/src/services/importHistoryService.js
server/src/services/importHistoryService.test.js
server/src/services/marketDataService.js
server/src/services/marketDataService.test.js
```

## Later concurrent Finance Tracker activity

After the successful immediate cleanup readback, a later read at about 18:19 UTC
observed Finance Tracker #39 closed/Done (closedAt 18:11:47Z) and #40 In Progress.
These changes occurred outside this task; this task only deleted incorrect Project
#12 memberships and did not issue any Finance Tracker state/field mutations.
Their legitimate Project #1 memberships and updated values are retained, not
reset to the earlier snapshot. The JSON distinguishes the cleanup snapshot from
this later activity. Trading Journal #1-#7 remained at the required states with
only the explicitly requested Feature title and #4 acceptance body changes.
