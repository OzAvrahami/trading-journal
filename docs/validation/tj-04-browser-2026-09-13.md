# TJ-04 real-browser follow-up — 2026-09-13

**21 recorded browser scenarios passed, with zero uncaught page errors. The owner
accepted the application review/tests on 2026-09-13. Verification of the final
reviewed/pushed fix SHA remains pending.** Issue
#4 remains open in Verify; #5/#6 remain Backlog. This follow-up supersedes the
browser-availability gap in the [September 12 report](tj-04-neon-2026-09-12.md),
without changing that dated API/recovery evidence or claiming production readiness.

## Code and destination

Branch `chore/tj-02-neon-preparation`, HEAD
`2821435dc609ad85d3447cd22f799fb17a508968`. The starting 19 pending paths matched
the previous inventory; the five recorded implementation hashes matched. Existing
TJ-03/TJ-04 files, import/quote corrections and migration 020 were preserved.
The browser used the current working tree, including those uncommitted fixes and
the pagination correction below; these results do not describe the committed SHA alone.

The actual Vite client runs at **http://127.0.0.1:5173**, with the actual Express
API at `http://127.0.0.1:3001`. The normal client `/api` proxy, loopback hostname,
development CORS/cookie contract, existing navigation and JWT authentication are
used. An isolated task runner clears inherited database/provider settings and
loads protected local credentials; it does not replace application routes or UI.
The final reachability check returned client HTML 200, real login 200 and
authenticated profile/accounts 200 (three review accounts), beyond a health probe.

| Identity | Verified value |
| --- | --- |
| Neon project / branch | `delicate-fire-79141899` / `br-green-cake-arg1asaf` |
| Direct endpoint | `ep-bold-fire-aryb3eiv.c-4.us-west-2.aws.neon.tech` |
| Database | `trading_journal_validation` |
| Login / effective role | `tj_backend` / `tj_owner` through reviewed application startup |
| TLS | Authorized certificate and hostname, `verify-full` |
| Migration 020 | Checksum `21f5ba958704ca267068db7f8024ef465e8d5a8bc67d75df7fb7ed840c6b6cc7`; applied `2026-09-12 12:24:44.138055+00` |

All 20 ledger records and the full-row fingerprints of every previously recorded
business row remain unchanged. The independent initialization database and recovery
resources were neither written nor retested. There was no provisioning or provider
deletion. The startup snapshot in the JSON is a restart after review user A signup;
its three user IDs are not an assertion that all three existed before this task.

## Actual browser results

Installed **Google Chrome 152.0.7977.83**, Playwright **1.60.0**, headless isolated
persistent profile. Connected browser integrations returned an empty inventory;
the existing installed browser/automation was used without installation. Screens
were inspected at desktop and 390 × 844 CSS pixels. This is actual Chrome evidence,
separate from the earlier API/jsdom checks; it is not physical-device testing.

The [structured evidence](tj-04-browser-2026-09-13.json) contains every scenario,
network result, synthetic fixture identifiers/inputs, destination/ledger readback,
preservation comparison and final authenticated reachability check.

| Check | Expected and actual result |
| --- | --- |
| Signup and explicit first account | UI signup 201; authenticated dashboard/accounts/portfolio 200 and empty; first account created through the real dialog. |
| Login, reload and logout | Ordinary login succeeds; reload restores the session and rotates the HttpOnly cookie; logout clears it and returns to login. Local cookie is SameSite Strict, Secure false, domain 127.0.0.1. |
| A → B isolation | A logout followed by B signup/login shows B's empty accounts. A real A account response held until after B login was then delivered; it did not repopulate B's cache. |
| Expired access and stale refresh | A browser-only expired access-token input produced ten real protected 401 responses, exactly one real refresh 200 and successful retries. A controlled stale cookie produced one refresh 401, cleared the cookie and returned to login without a loop. |
| Temporary failures | Controlled 503 and transport failures each produced two bounded trade requests, preserved the signed-in user/cookie and recovered after removal of the control. |
| Language and dates | English ↔ Hebrew updates LTR/RTL without logout. Hebrew account/journal labels and trade dates render. TJLONG's 07:00Z entry displays 10:00 in Jerusalem. Broader DATE/instant/DST/precision oracles remain the September 12 API evidence. |
| Responsive navigation/forms | Desktop table and mobile trade cards; 390px document width equals viewport. Account dialog fits horizontally and scrolls to submit; mobile More navigation opens investment holdings. |
| Account-scoped CSV feedback | Actual TopstepX UI import inserts one trade. Uploading it again blocks the primary account with duplicate feedback; selecting the other owned account enables import. No second-account import was committed in this browser exercise. |
| Unavailable quotes | The actual local API has no Finnhub key. UI displays dated manual price 110, holdings 220 and unrealized 20. |
| Current → stale quotes | Controlled browser quote 120 shows holdings 240/basis 200. After advancing only the isolated browser clock beyond the cache interval and returning an explicit stale 503, UI uses manual 110/holdings 220 instead of retained live data. No ledger price was changed by this simulation. |

All browser failure/price controls were removed by closing the isolated automation
context. The real client/API remain running for owner review. The local API was
restarted after automated traffic to give the owner normal rate-limit headroom;
the policy and signing secret were not changed. No rate-limit failures occurred.

## Demonstrated correction and focused rerun

The Hebrew trade pagination summary initially rendered the English fallback.
Added `trades.showing` to both locale dictionaries and used `dir="auto"` on the
summary. The real-browser rerun rendered **מציג 1–3 מתוך 3 עסקאות**. Only these
three additional application files changed:

- `client/src/i18n/locales/en.js`
- `client/src/i18n/locales/he.js`
- `client/src/pages/Trades.jsx`

`npm test --prefix client -- --run src/pages/Trades.test.jsx src/i18n/i18n.test.jsx`
passed **15 tests in two files**. Existing tool deprecation/data-age warnings did
not require dependency changes. No completed financial/API/recovery suite was rerun.

Harness corrections were not product failures: Vite must start with the client
working directory for its Tailwind config; a mutable quote-control closure was
replaced with an explicit stale response handler. Both were corrected before their
passing assertions. The initial pagination observation is retained in JSON.
A nonblocking wording observation remains for review: Holdings uses “כל תאריך”
for the general “All” filter option. No blanket claim of perfect localization follows.

## Retained synthetic review data

Two new users, A (Hebrew review display name) and B (empty), were registered
through the actual UI. A owns two trading accounts, one linked investment account
and portfolio, three trades (including the browser CSV import), a linked Hebrew
journal, one instrument, deposit/buy transactions and a dated manual price.
Refresh sessions, preferences and import history/rows created by these flows are
also task-owned fixtures. IDs and inputs are in the structured evidence. All data
is isolated validation data and must never be copied into production.

| Fixture | Independent expected result / review value |
| --- | --- |
| TJLONG: long 100→110 ×2, fee 1, risk 10 | Gross 20; net 19; R 1.9 |
| TJSHORT: short 110→105 ×3, fee 1, risk 10 | Gross 15; net 14; R 1.4 |
| TJCSV: supplied gross 12, fees/commission 3 | Net 9; primary account aggregate net 42 after import |
| TJVIEW: deposit 1000, buy 2×100, manual 110 | Cash 800; basis 200; holdings 220; unrealized 20; total 1020 USD |

Disposable credentials are provided only in the private local handoff outside the
repository. They are absent from GitHub, this evidence and screenshots. The private
CSV is already imported only into A's primary account, ready for owner preview of
same-account blocking versus another account's eligibility. Owner need not manipulate
tokens or browser storage; those failure scenarios were exercised by automation.

Screenshots (email masked): [desktop accounts](tj-04-browser-2026-09-13/accounts-he-desktop.png),
[mobile trades](tj-04-browser-2026-09-13/trades-he-mobile.png),
[manual fallback](tj-04-browser-2026-09-13/holdings-he-fallback.png).
The account screenshot precedes the CSV import (1033 versus final 1042); it is a
dated flow capture, not the final aggregate oracle.

## Acceptance and remaining limits

Issue #4 items 2 and 6 now have combined actual-browser plus prior API evidence
and can be checked. Items 1/3/4/5 retain their earlier evidence. Item 7 remains
open pending **verification of the final reviewed/pushed fix commit**. The owner
subsequently accepted the application review/tests on 2026-09-13; that explicit
acceptance is separate from the automated browser results and production readiness.

Local HTTP cookies do not verify production cross-site HTTPS/Secure/SameSite
behavior. Actual Finnhub connectivity, physical mobile/other browser engines,
Railway latency/load and production launch remain unverified downstream. Controlled
price/network responses prove UI behavior, not provider availability. The prior
restricted recovery evidence and its limits are unchanged. No production destination
or application was connected, and no operational recovery commitment is inferred.

All package/lockfile versions remain 1.0.0. No staging, commits, pushes, merges,
tags, releases, deployments or production configuration/secret changes occurred.

## Browser-completion file and metadata snapshot (before owner acceptance)

There are 27 pending paths: the 19 previous paths are preserved, plus the three
pagination source files, this report/JSON and three screenshots. This pass also
updates CHANGELOG and the existing development standard, initialization guide,
plan, backlog and September 12 report with dated follow-up links. The prior TJ-03
and TJ-04 structured evidence, fixture specification, migration 020 and earlier
implementation/test files remain byte-for-byte unchanged. Complete path inventory
and current implementation hashes are in the JSON.

UTF-8/JSON parsing, local Markdown links, whitespace, unchanged 1.0.0 manifests/
lockfiles and the empty index passed. A protected-value/JWT scan found no credentials
in pending text files. Ignored local helpers, profiles, logs and private handoff
files are excluded. GitHub issue body/comment were read back, with #4 open/Verify,
#3 closed/Done and #5/#6 open/Backlog; labels, priorities, milestone, unique Project
memberships and native dependency order were preserved at browser completion.
Owner visual acceptance was pending at that snapshot and is now received. The
[current handoff](tj-04-handoff-2026-09-13.md) contains the newly reviewed inventory
and final pushed-commit gate. Historical fixture inventories are not a current
claim that every Neon record is synthetic: the owner has since registered and
created accounts, and those records are protected.
